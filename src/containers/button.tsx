import {Header, Button, Sidebar,  NavigationBar, NavigationBarItem,ButtonSubmit} from "../components/index";
let ButtonView = React.createClass({
    render: function () {
        return (<ButtonSubmit>确认按钮</ButtonSubmit>)
    }
})
export default ButtonView;